/*
 * JourneyPage Messages
 *
 * This contains all the text for the JourneyPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.JourneyPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the JourneyPage container!',
  },
});
