// import { selectExperimentsPageDomain } from '../selectors';

describe('selectExperimentsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
