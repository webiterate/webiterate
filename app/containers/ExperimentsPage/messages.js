/*
 * ExperimentsPage Messages
 *
 * This contains all the text for the ExperimentsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ExperimentsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExperimentsPage container!',
  },
});
