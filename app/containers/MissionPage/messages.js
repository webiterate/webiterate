/*
 * MissionPage Messages
 *
 * This contains all the text for the MissionPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MissionPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MissionPage container!',
  },
});
