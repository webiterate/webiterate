import styled from 'styled-components';
import { accent } from '../../styles/helpers/variables';

const H1 = styled.h1`
  color: ${accent};
  font-size: 2em;
  margin-bottom: 0.25em;
`;

export default H1;
